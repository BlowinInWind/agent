[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.4.7",
    "notebook>=7.5.4",
    "tavily-python>=0.7.22",
]
